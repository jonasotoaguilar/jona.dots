# {Project Name}

{README template: the project's front door — what it is, who it helps, and how to reach first success — not a technical manual. Replace every `{placeholder}`; omit sections marked (optional) when they do not earn their place. Adapt titles, order, and depth to the project type and stack (CLI, library, web app, single-purpose) — this is a base shape, not a rigid mold; only the hero, the happy path first, and the license badge footer are invariants.}

<div align="center">

<img width="{banner-width}" height="{banner-height}" alt="{Project Name} banner" src="{banner-path}" />

<h1>{Project Name}</h1>

<p><strong>{Project Name} — {one-line tagline}.</strong></p>

<p>
<a href="https://github.com/{owner}/{repo}/releases"><img src="https://img.shields.io/github/v/release/{owner}/{repo}" alt="Release"></a>
<a href="LICENSE"><img src="https://img.shields.io/badge/License-{SPDX}-blue.svg" alt="License: {SPDX}"></a>
<img src="https://img.shields.io/badge/{Runtime}-{version}-{color}?logo={logo}&logoColor=white" alt="{Runtime} {version}">
<img src="https://img.shields.io/badge/platform-{platforms}-lightgrey" alt="Platform">
</p>

</div>

---

> [!IMPORTANT]
> {Only when the reader must know this before anything else: stable version to install, prerelease channel, or a hard prerequisite. Otherwise omit.}

## What It Does

{1–2 paragraphs: what the project is, who it is for, and the problem it solves.}

**Before**: {how the world looks without the project}

**After**: {how it looks with the project}

### Supported Integrations (optional)

{Include only when the project integrates multiple agents, platforms, or services. Chunk into a table, not prose.}

| {Agent / Platform} | {Model} | Key Feature |
| --- | --- | --- |
| **{Name}** | {Model} | {Feature} |
| **{Name}** | {Model} | {Feature} |

---

## Quick Start

### Install (recommended)

> [!NOTE]
> {Install prerequisite — runtime or package manager. Optional.}

**{Platform 1}**

```bash
{install command}
```

**{Platform 2}**

```powershell
{install command}
```

<details>
<summary><strong>{Alternative installs, channels, or scope options}</strong></summary>

{Alternatives — package managers, version channels, scopes. Progressive disclosure: happy path first, details collapsed.}

</details>

### {Configure / Verify initial setup} (optional)

{Include only when setup continues after install or a first-success check exists. Chunk recurring commands into a table.}

| {Command} | What it does | When to re-run |
| --- | --- | --- |
| `{command}` | {What it does} | {When to re-run it} |

### Local Development (optional)

{Include only when the project has distinct local-development steps — dev servers, seed data, environment profiles. Otherwise omit or link to the Documentation table.}

```bash
{local dev command}
```

### Troubleshooting (optional)

{Include only for recurring or high-value failure recovery — known failure modes, common errors, rollback. Otherwise omit or link to the Documentation table.}

{Common issue} — {fix, or link to the doc that covers it}

---

## Configuration (optional)

{Include only when the project has configurable options — env vars, flags, ports. Chunk into a short table; link the full reference. Never hide required options.}

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `{option}` | `{type}` | `{default}` | {What it does} |

---

## Core Workflow (optional)

{Include only when the project has a meaningful operational/user workflow — the main end-to-end flow a user repeats. Outcome-oriented: the result the reader is working toward, not a command dump. Happy path only; details link into the Documentation table.}

1. **{First action}.** {What it achieves}
2. **{Second action}.** {What it achieves}
3. **{Expected outcome}**

### The flow at a glance (optional)

{Add a small mermaid diagram only when it clarifies the order.}

```mermaid
flowchart TD
    {A} --> {B}
    {B} --> {C}
```

---

## Key Features You Should Know About (optional)

{Only differentiating features, each with depth and a short example — not a catalog dump. 1–4 features.}

### {Feature 1}

{Why it matters, minimal example.}

```bash
{example command}
```

### {Feature 2}

{Why it matters, minimal example.}

```bash
{example command}
```

---

## Documentation

{Include only when the project has deeper docs. Map reader tasks to the doc that starts each task — recognition over recall. Never duplicate docs already linked in the body; link, don't restate.}

| Your task | Start here |
| --- | --- |
| {Understand the mental model} | [{doc}]({path}) |
| {First workflow} | [{doc}]({path}) |
| {Troubleshoot / recover} | [{doc}]({path}) |
| {Report a vulnerability} | {SECURITY.md — only when it exists} |
| {Extend / contribute} | [{doc}]({path}) |

---

## Community Highlights (optional)

{Include only when the project has a community or ecosystem.}

### Community Integrations (optional)

- [{name}]({url}) — {what it does}

### Contributors (optional)

{Link to CONTRIBUTORS.md, or a contributors image, or both.}

---

## Next Steps

- **{Reader role or state}** — {action and link}
- **{Reader role or state}** — {action and link}

---

<div align="center">
<a href="LICENSE"><img src="https://img.shields.io/badge/License-{SPDX}-blue.svg" alt="License: {SPDX}"></a>
</div>
