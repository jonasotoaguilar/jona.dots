# Runtime state — no versionable
*.log
*.sock
.plugins.lock
session.json
release-notes.json
config.toml.bak.*

# Plugin cache (re-clonable via `herdr plugin install`)
plugins/github/

# Generated plugin config / caches
.atl/.skill-registry.cache.json
plugins/config/herdr-lazygit/layout-*.yml
plugins/config/herdr.collie/tailscale-managed-handler
